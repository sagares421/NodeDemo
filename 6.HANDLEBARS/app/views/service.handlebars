{{> nav}}

<div id="main">
    <div class="container" style="margin-top: 100px;">
        <div class="card">
            <div class="card-body">
                <h1 style="text-align: center;">Welcome {{name}}</h1>
            </div>
        </div>
    </div>
</div>

{{> footer}}